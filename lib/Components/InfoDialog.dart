import 'package:flutter/material.dart';

class InfoDialog extends StatelessWidget {
  final String lesson;

  InfoDialog({required this.lesson});

  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}
